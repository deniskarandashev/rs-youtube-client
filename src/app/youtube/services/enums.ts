export enum ThumbnailType {
    DEFAULT = "default",
    MEDIUM = "medium",
    HIGH = "high",
    STANDARD = "standard",
    MAXRES = "maxres",
}

export enum StatusColor {
    BLUE = "rgb(0, 0, 255)",
    GREEN = "rgb(0, 150, 0)",
    YELLOW = "rgb(239, 228, 0)",
    RED = "rgb(255, 0, 0)",
}

export enum SortingOrder {
    NONE,
    ASC,
    DESC,
}

export enum SortingBy {
    NONE,
    DATE,
    VIEWS,
    TEXT,
}
